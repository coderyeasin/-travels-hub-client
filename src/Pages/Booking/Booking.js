import React from 'react';

const Booking = () => {
    return (
        <div>
            <h2>This is Booking--should every card details and booked</h2>
        </div>
    );
};

export default Booking;